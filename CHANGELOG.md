# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/)；格式参考 [Keep a Changelog](https://keepachangelog.com/)。

## [Unreleased]

## [0.8.0] - 2026-08-03

### Added

- 制作页新增 Vmoe 卡拉 OK 字幕库官方搜索入口，可在应用内完成站点要求的 reCAPTCHA，
  下载带逐字特效的 ASS 后直接作为歌词/字幕项目载入；程序不会绕过验证码或代替用户提交。
- 新增 QQ 音乐公开歌词适配器：网页和 `qqmusic` 命令可解析单曲页/分享文字，读取行级
  LRC、歌曲信息与可用翻译，并导出 LRC、增强 LRC、SRT、VTT、ASS 和项目 JSON；该入口
  不请求音频、账号或 Cookie。
- 制作页可在没有上传歌词时直接采用 QQ 音乐公开 LRC，并保留来源、单曲链接和翻译元数据。

### Changed

- 纯文本歌词匹配覆盖率低于安全阈值时，校准流程不再直接终止：现在会保留所有歌词，利用
  已识别锚点插值，完全无法匹配时按检测到的演唱区间生成可编辑的保底时间轴。
- 低覆盖率工程会明确列出未匹配歌词、Whisper 实际识别语言与置信度、当前模型和人声分离
  状态，并给出切换 `medium` / `large-v3` 或启用 Demucs 的建议。
- 网页扩展为七个功能区，在线歌词来源统一集中在制作页，并新增独立的 QQ 音乐歌词导出页。

## [0.7.1] - 2026-08-03

### Added

- Windows “首次安装.bat”现在会在项目内自动准备固定版本并校验 SHA-512 的私有
  Python 3.12.10 运行时，再创建 `.venv`；新电脑无需预装系统 Python 或 Conda，且不会
  修改全局 `PATH`。

### Fixed

- 三个 Windows 批处理入口改为纯 ASCII 命令，并通过 `.gitattributes` 强制检出为 CRLF，
  避免中文系统代码页把 UTF-8 提示拆成 `ffmpeg.org`、`errorlevel` 等错误命令。

## [0.7.0] - 2026-08-03

### Changed

- 编辑器的普通“保存并导出”完成后，会自动把同批 JSON 工程载入 MV 制作页；制作页也会
  立即显示文件名、歌词行数和时间轴状态，并明确推荐 JSON 以保留完整逐词编辑信息。
- 制作页同时存在上传文件和旧的粘贴歌词时，始终优先采用新选择的文件，避免重新制作时
  悄悄回退到修改前的文本。
- “确认校准并开始制作 MV”现在会在交接成功后直接继续最终渲染；若制作页尚未选择 MV，
  则保留本次歌词工程和音频，切回制作页并明确提示只需补选 MV。

### Fixed

- 修复网易云原始 YRC 在标点处已有约 10ms 轻微重叠时，删除同句其他词会在导出阶段被
  误判为新时间重叠的问题；原有时间保持不变，真正新产生的重叠仍会被拦截。
- 修复删除歌词行后当前行号超出新工程范围时，“确认校准并开始制作 MV”可能失败并让
  制作页继续沿用旧歌词的问题。
- 重新载入 Karaoke Forge 生成的 ASS 时会恢复其中的 `\kf` 逐词时长，不再把已有
  KTV 扫色退化为行内平均时间；同批 JSON 仍是保留翻译、注音和编辑状态最完整的首选。
- 网页编辑导出和交接错误不再只弹出瞬时提示；界面会显示具体原因，并把完整堆栈追加到
  `outputs/karaoke-forge-errors.log`，便于后续追查。
- 修复载入以前编辑好的歌词工程后，点击“确认校准”只完成文件交接、没有进入最终 MV 制作
  的问题；失败的交接也不会误触发旧工程渲染。

## [0.5.0] - 2026-08-01

### Added

- Windows 新增独立的“安装人声分离（Demucs）.bat”：可选推荐的轻量 CPU 版或从
  PyTorch 官方源安装 CUDA 版；显卡版下载失败不会破坏 CPU 回退。
- 新增 `scripts/verify_editor_workflow.py` 自动编辑验证：用真实歌词项目和音频模拟载入、
  逐句试听、删词且保留其他时间、无修改撤销、修改后撤销、拖动词边界和全格式导出。
- 新增 `scripts/verify_web_session.py`，通过真实运行的 Gradio 会话复现载入、自动进入
  下一句、生成试听、删词、撤销和导出，覆盖仅调用内部函数无法发现的会话状态问题。

### Changed

- Demucs 可选依赖限定为 4.x；网页选项和环境检查会显示实际是 NVIDIA、CPU、未安装或
  安装异常，避免“装了 Demucs”却误以为正在使用显卡。
- 人声分离跟随页面选择的运行设备，并把模型首次下载与分离输出持续显示在任务进度中；
  CUDA 选择错误、显存不足和模型下载失败都会给出可操作的中文提示。
- Demucs 模型下载复用项目缓存，并在 Hugging Face 不可连接时快速切换到官方模型镜像；
  Windows 同时使用 certifi 证书包，避免系统证书异常导致首次模型下载直接失败。

### Fixed

- 逐词编辑器不再在载入时把所有词块时间先四舍五入到毫秒；现在删除或改写一个词块时，
  没有动过的其他词块会原样保留底层时间，而画面仍使用易读的小数位显示。
- 行首尾没有变化时不再把整句词块按 100% 比例无意义地重算，避免仅保存文字、注音或
  表格时由浮点换算悄悄改变逐字时间。

## [0.4.0] - 2026-08-01

### Added

- “制作卡拉 OK MV”新增一次上传的两阶段流程：音频、MV、歌词和网易云链接只需在制作页
  提交一次，即可先生成可试听、可微调的 KTV 校准工程；完成后自动返回制作页渲染成片。
- 生成校准工程时会把日语振假名和英文片假名读音写入项目 JSON，便于渲染前逐词修改，
  同时沿用网易云原文、逐字时间轴和中文翻译。
- 编辑器支持点击歌词表格行后自动播放对应句子；逐词时间以可点击试听的词块显示，可拖动
  黄色边界调整每个词的开始/结束时间，解决拖长音或局部识别过快的问题。
- 逐词时间条新增随音频移动的红色播放头、当前词高亮、拖动撤销/重做，以及按实际逐词
  边界驱动的 KTV 预览实时扫色。
- 长句播放时逐词时间条会自动滚动跟随红色播放头，并提供“前一段/后一段”按钮；KTV
  预览移到编辑区顶部并保持粘性显示，调整下方时间和注音时无需来回滚动页面。
- 逐词时间条增加缩小、适应全句和最高 8 倍放大，短字与快速唱词也能获得足够的拖动空间。
- 编辑器改为可滑出的歌词总览抽屉，微调区占满主要画面；点击序号选择歌词后抽屉自动
  收起，点击其他单元格仍可在抽屉中编辑。隐藏/显示、删除和撤销/重做改为右侧明确
  按钮，不再依赖浏览器右键事件。
- 编辑器升级为固定满屏单页工作台：顶部工具栏、KTV 预览、音频、逐词时间轴、当前句
  操作和注音并排显示，不再需要滚动整个网页；在预览或时间轴上按 `Ctrl + 滚轮` 可分别
  缩放歌词字号或时间轴精度。
- 歌词总览支持右键弹出轻量菜单，可隐藏/显示、向上或向下插入新行、删除，并支持撤销；
  当前句试听新增循环开关、自动进入下一句和 0.5×–2× 倍速控制。
- 逐词时间条中的文字现在可以直接编辑；清空一个词块并保存即可删除多余字或空格，其他
  词块的开始/结束时间保持不变。编辑文字时仍可正常输入空格。
- 逐词时间条支持右键删除单个词块，删除后立即从画面移除并自动保存，不会留下红色
  占位；放大后可以按住时间轴空白区域左右拖动浏览。
- 红色播放头现在拥有独立拖动命中区，可直接拖到当前句的对应播放时间；拖动期间会暂停
  自动跟随，松手后按原播放状态继续。

### Fixed

- 制作页未上传独立歌曲音频时，会先检测并复用 MV 内嵌完整音轨来生成校准工程和最终
  视频；只有 MV 确实无音轨或无法检测时才要求上传音频，也不再为有声 MV 额外请求
  网易云音频。
- 精修普通 LRC/SRT 的句内逐字时间时，会原样保留带时间戳的空白间奏行，不再因这些
  无需识别的行被对齐器跳过而误报“没有保留全部歌词行”。
- Whisper 模型未缓存且 Hugging Face 连接超时时，“自动”逐字时间精修会保留已有行级
  时间轴继续生成，不再让整项任务失败；“强制”策略仍会明确报错。模型加载现在优先使用
  本机缓存，并给出可操作的中文提示。
- “强制”逐字时间精修改为按每句原时间窗口检查，只采纳高覆盖、高相似且与可信源时间
  没有大幅冲突的修正；低质量识别和重复副歌不再覆盖网易云 YRC 或人工逐字时间。
- Whisper 下载器无法提供可靠下载比例时改为不定进度，并明确显示“无法提供可靠百分比”；
  不再用固定的 `50.0%` 冒充模型下载或识别进度。
- 修复重新载入 Karaoke Forge 自己导出的 ASS 时，把双行预告、翻译和注音事件重复识别为
  歌词行的问题；现在只回读真实演唱行，并恢复可匹配的翻译与整行注音。
- 修复编辑器 KTV 预览中带 `<ruby>` 振假名的汉字和读音未继承当前行颜色、在深色背景上
  近似消失的问题。
- 歌词表格禁用不稳定的右键行选择，避免右键触发跳行、重复刷新或关联组件显示“错误”。
- 播放同步改为读取 Gradio WaveSurfer 的实际高精度进度，不再误监听未参与波形播放的隐藏
  `<audio>`；红色播放头、当前词高亮、KTV 扫色和词块单独试听现在控制同一个播放器。
- 修复“循环当前句”不回到句首，以及关闭循环后只切换歌词却不继续播放的问题；现在使用
  播放器自身的循环和播放结束事件控制当前句及下一句，并支持在非文字输入区域按空格
  暂停/继续，不会再次激活上一个按钮。
- 修复播放控制误读隐藏音频元素后，KTV 扫色、红色播放头和当前词高亮无法跟随实际波形
  进度的回归问题；试听进度重新统一读取正在发声的 WaveSurfer 波形。
- 逐词修改、行首尾微调和注音保存现在都会建立带原行号的撤销快照；自动换句前会保存尚未
  点击“保存”的逐词修改，换到下一句后仍可撤销回修改行。没有修改时点撤销只显示提示，
  不再弹出错误。
- 当前句播放时会后台预生成下一句试听片段，纯切句不再重绘整张歌词表；试听缓存加入音频
  路径、大小和修改时间指纹，并通过目标锁和原子替换避免同名音频串缓存或并发写坏文件。
- 逐词文字、边界和注音开始编辑时会先暂停试听；预览只响应真实用户输入，不再因自动
  切句的组件更新并发触发多次旧预览，减少切句卡顿并避免旧画面覆盖保存或撤销结果。
- 单词试听的停止计时器会绑定创建它的歌词行，切句后不再误停新音频；试听末词时会在
  句尾前安全暂停，避免误触发自动下一句。插入/删除/隐藏/撤销或微调句首尾后会立即换成
  当前范围的新试听片段，自动切句前也会一并保存尚未点击保存的注音草稿。
- 自动换句门禁现在以“结束事件所属行”为准，并只屏蔽同一行的单词试听或正在编辑事件；
  极短句、2 倍速、空格继续播放、拖动红色播放头以及刚关闭循环时，真实的句尾事件不再
  被固定延时窗口误吞，也不会由旧行的迟到事件覆盖手动选中的新行。
- 在歌词总览直接修改当前行的开始/结束时间时，会同时重建逐词时间轴并换用新范围的试听
  片段；只改原文或翻译不会无故重载并自动播放音频。所有编辑动作共用一个有序音频刷新
  入口，避免快速连续微调时旧片段后返回并覆盖新片段。
- 删除逐词文字后直接保存注音、导出全部格式或交给 MV 制作时，会先统一保存当前行的逐词、
  注音和表格草稿；旧注音区间不会被重复套用，也不会漏掉尚未单独点击“保存”的删字结果。

后续计划见 [TODO.md](TODO.md)。

## [0.3.0] - 2026-07-31

### Added

- 网页新增“歌词与注音编辑”页签，可逐行修改原文、翻译、开始/结束时间和显示状态。
- “歌词与注音编辑”升级为渲染前 KTV 预演：可上传歌曲音频、截取当前句试听、用
  ±0.1 秒按钮微调行首/行尾、查看当前句与下一句双行预览，并将确认后的 JSON 和音频
  一键带入最终 MV 制作页。
- 支持逐词注音表格与整行 `pronunciation` 字段；保存后字幕预览立即更新，结构化注音
  会写入项目 JSON 并用于 ASS。
- 歌词行可选择“显示”“隐藏”或“删除”；隐藏行保留在 JSON 中以便恢复，但不会进入
  LRC、SRT、VTT、ASS 或视频，删除行则从导出项目中彻底移除。
- MV、时间轴、网易云网页流程和 `make`/`netease` 命令统一支持逐字时间精修策略：
  `off`、`auto`、`force`。

### Changed

- 编辑歌词文本时会重建与新文本一致的合成逐字时间；仅修改行首/行尾时会按比例缩放
  原有逐字边界。
- JSON 新增向后兼容的 `hidden` 和 `pronunciation_units` 字段。

### Fixed

- 上传独立音频和无声 MV 时，自动同步会跳过不存在的 MV 音轨，并按手动偏移继续合成，
  不再因 FFmpeg `0:a:0` 流映射错误而中止。
- 网页制作 MV 可直接选择输出目录；FFmpeg 遇到磁盘空间不足时会清理本次残缺视频，并返回
  可操作的中文提示。
- 输出目录位于项目外部或其他磁盘时，会将其加入本机 Gradio 文件白名单，并把网页缓存
  放到输出目录同盘，避免成片已经完成却在预览/下载阶段报错。
- KTV 双行字幕改为逐句滚动：A/B 显示时，A 唱完进入 B 后，上方会立即预告 C，而不是
  等 B 唱完才整组切换到 C/D。

## [0.2.0] - 2026-07-31

### Added

- 网易云链接支持从本机 Chrome、Edge、Firefox 或 Brave 读取现有登录会话，检测该曲
  实际可用的 VIP/SVIP 音质，并只使用账号本来具有的最高播放权限；Cookie 不落盘。
- 新增多窗口音轨指纹匹配，可自动跳过 MV 片头/片尾剧情并定位歌曲开始位置，同时拒绝
  错误歌曲版本。
- 网易云中文翻译可写入项目 JSON，并在 ASS 中固定显示于画面顶部。
- ASS 原文采用传统 KTV 双行布局：两句成组、左上右下交错，当前句逐字变色。
- 日语汉字可自动生成平假名振假名，英语单词可自动生成片假名读音，并随原文扫色。
- 网易云歌词优先使用 YRC 真实逐字时间；普通行级 LRC/SRT 可根据演唱音频精修句内
  变速时间，并保持原有行首、行尾不动。
- 网页字幕样式区新增 16:9 实时预览，字体、字号、颜色、翻译和位置可即时查看。
- 会员曲目只返回试听片段时，“制作 MV”可自动回退到上传 MV 的完整内嵌音轨。

### Fixed

- 网易云公开信息请求改用 Certifi CA，修复部分 Windows 环境的 `ASN1: NOT_ENOUGH_DATA`
  证书加载错误。
- Chromium 浏览器正在占用 Cookie 数据库时，返回可操作的中文提示。
- 默认关闭面向语音的 VAD，避免伴奏较强的歌曲在中途被误判为无人声而导致歌词对齐覆盖率过低。
- 为纯文本歌词保留段落空行作为 Whisper 提示，改善重复副歌和尾声的识别连续性。

## [0.1.0] - 2026-07-30

### Added

- `align`：使用 faster-whisper 时间戳对齐用户歌词；
- `convert`：LRC、增强 LRC、SRT、VTT、ASS、JSON 相互转换；
- `render`：通过 FFmpeg 烧录逐词高亮字幕并可替换音轨；
- `make`：串联识别、对齐、格式导出和视频渲染；
- `web`：面向非技术用户的本地可视化工作台；
- Windows 首次安装与双击启动脚本；
- 网易云公开单曲链接解析、LRC 获取与匿名公开音频适配器；
- 会员歌曲可配合用户合法导出的本地标准音频，不读取 Cookie、不处理 NCM；
- 一键 MV 流程可直接采用网易云公开歌词；
- 可选 Demucs 人声分离；
- `doctor` 环境检查；
- 中英文 README、算法/格式文档、测试与 GitHub Actions。
