# Contributing

感谢你愿意改进 Karaoke Forge。

## 开发流程

1. Fork 仓库并从 `main` 创建短分支。
2. 创建 Python 3.10+ 虚拟环境。
3. 运行 `pip install -e ".[dev]"`；涉及真实对齐时再安装 `.[align]`。
4. 修改代码，并为行为变化增加测试。
5. 运行：

   ```bash
   python -m pytest
   ruff check src tests
   ```

6. 在 Pull Request 中说明使用的系统、Python 版本、示例命令和验证结果。

## Issue

报告对齐问题时，请提供：

- 操作系统、Python 版本和 `karaoke-forge doctor` 输出；
- 使用的模型、语言、设备和计算类型；
- 命令的完整参数；
- 匹配覆盖率和完整错误信息。

请不要上传没有分发授权的歌曲、歌词或 MV。可以提供自行制作的最小复现音频，或只提供脱敏后的时间轴 JSON。

## 代码约定

- 面向用户的错误要给出下一步解决方法；
- 核心格式转换应保持无重型运行时依赖；
- 新格式必须同时包含解析、导出和往返测试；
- 时间统一使用秒，写出时才做格式精度转换；
- 不在测试或示例中提交受版权保护的媒体。

提交贡献即表示你同意按本项目的 MIT License 发布该贡献。
