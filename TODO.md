# Karaoke Forge 待办事项

这里记录尚未实现的功能。已完成的版本变化请查看 [CHANGELOG.md](CHANGELOG.md)。

## 已完成（0.12.4）

- [x] **面向电脑新手的网易云一键登录**
  - 程序打开独立本机 Edge profile 的网易云官方页面，不影响日常浏览器窗口；
  - 登录态可在后续制作中复用，不再要求关闭 Edge、复制 Cookie 或安装 Firefox。
- [x] **歌曲实景字幕预览**
  - 有 MV 时使用歌词对应时刻的真实画面帧，没有 MV 时使用当前封面、背景和唱片/频谱场景；
  - 使用实际歌词、翻译、上下排与逐字扫色，样式参数即时更新并随最近工程恢复。

## 已完成（0.12.3）

- [x] **Edge/Chrome 保持开启时使用网易云会话**
  - 网页可手动提供 `MUSIC_U`，无需关闭当前浏览器或另外安装 Firefox；
  - 手动会话优先于浏览器数据库读取，只限定在网易云域名并强制通过 HTTPS 使用；
  - 登录凭据不写入工程、输出文件或日志，错误与下载器提示都会执行脱敏。

## 已完成（0.12.2）

- [x] **网易云会员音频稳定下载**
  - 已登录账号返回母带或 Hi-Res 权限时，校准与 MV 制作优先使用兼容的标准音频，避免大型
    母带文件导致不必要的超时或节点失败；
  - 增加连接超时、自动重试和脱敏后的真实错误摘要，不再把网络下载错误误报成会员无效；
  - 使用真实 Firefox SVIP 会话验证完整歌曲下载、时长探测与非试听片段判定。

## 已完成（0.12.1）

- [x] **网易云空白间奏与歌词行删除修复**
  - 保留来源中带时间的空白间奏，普通歌词清空后可用删除按钮明确移除并正常撤销；
  - 拦截网页产生的空音频占位文件，不再向用户显示 FFmpeg 的大段无效输入错误。
- [x] **校准工程复用网易云账号可播放音频**
  - 没有本地/MV 音频时，按用户明确选择的已登录浏览器获取账号有权播放的完整标准音频流；
  - 支持配置名和完整浏览器用户目录，会员、Cookie 缺失和试听片段提示彼此区分；
  - 不绕过账号权限，不转换或解密 NCM。

## 已完成（0.12.0）

- [x] **修复识别对齐忽快忽慢与逐段漂移**
  - ASS 导出完整保留首词延迟、词间停顿和绝对逐词起点，注音同步复用真实 token 时间；
  - Whisper 关闭容易造成重复循环和时间戳失步的跨窗口文本继承，改用覆盖全曲的短歌词热词；
  - 普通行级歌词使用可靠行首锚点做单调分段漂移校正，支持固定偏移、渐进漂移和局部变速；
  - 低置信、异常超长、孤立或局部覆盖不足的识别结果不再覆盖原时间，并在报告中显示校准质量。
- [x] **三档识别与逐行精准对齐**
  - 快速档使用 `small` + beam 3，默认均衡档使用 `large-v3-turbo` + beam 5；
  - KTV 精准使用 `large-v3` + beam 5，自动尝试 Demucs，并在失败时回退原音频；
  - 正式歌词按行交给 CTranslate2 强制对齐，未通过质量门的行保留粗对齐结果；
  - 短句对齐显式使用真实有效帧数，避免旧版特征接口的 30 秒预填充造成时间扩散与漂移。

## 已完成（0.11.0）

- [x] **原声与无人声伴奏双版本导出**
  - 最终导出区提供原声版和无人声伴奏版两个独立选择，可单选或同时选择；
  - Demucs 一次生成并复用人声与伴奏轨，同时导出时字幕画面只编码一次；
  - 工程保存导出选择和原始音频，下次打开可以继续切换或重新导出。

## 已完成（0.10.3）

- [x] **真正的黑胶唱片机布局**
  - 用大尺寸黑胶唱片、唱臂和唱针替换桌面 CD 播放器；
  - 专辑封面作为旋转的唱片中心标签，继续搭配封面柔焦背景和真实音频波形；
  - 旧工程保存的 `cdplayer` 布局会自动迁移，不需要重新选择样式。

## 已完成（0.10.2）

- [x] **多背景无 MV 视觉系统**
  - 默认根据每张专辑封面生成保留原色的柔焦流光背景；
  - 新增深空、日落玻璃、海盐水光和纸艺花园固定舞台，并加入低干扰慢速漂移；
  - 背景和五种唱片/频谱布局分开选择，组成 25 种效果，选择会随工程保存和恢复；
  - 新增高质感桌面 CD 播放器，专辑碟面在机身中旋转，默认搭配封面柔焦背景。

## 已完成（0.10.1）

- [x] **无 MV 的音频响应画面**
  - 本地或在线专辑图可生成星环舞台、午夜黑胶、极光唱片、银河频谱四种风格；
  - 内置原创深空舞台背景，动态元素避开底部歌词安全区；
  - 波形/频谱由歌曲音频实时驱动，并可单独关闭。
- [x] **自定义字体与持久工程**
  - TTF、OTF、TTC 字体随工程保存并交给 libass 加载，不要求系统安装；
  - 歌词、音频、MV/封面、字体和设置会保存为工程，下次打开自动恢复最近工程。
- [x] **过滤旧工程的英文注音**
  - 关闭英语片假名后，最终字幕会跳过已存的英文注音，同时保留日语读音。

## 已完成（0.9.1）

- [x] **分开控制英语自动注音**
  - 英语片假名可以独立关闭，不影响日语振假名、手动注音或在线来源提供的官方注音。
- [x] **只把 UtaTen 官方注音合并到自己的歌词**
  - 自己的正文、翻译和时间轴保持不变；原错误注音会先清除；
  - 按行序和相似度寻找候选，再逐字符确认 ruby 片段，只转移可验证的读音；
  - 多出、缺少或改写而无法确认的文字保持无注音，不进行自动猜测。

## 已完成（0.8.0）

- [x] **低覆盖率时仍生成可恢复的校准工程**
  - 匹配覆盖率低于安全阈值时不再终止 Step 03；已有识别锚点会继续用于插值，完全没有
    匹配时会按检测到的演唱区间生成可编辑的保底时间轴；
  - 工程保留全部正式歌词并直接进入编辑器，同时列出未匹配行、实际识别语言与置信度、
    当前模型、人声分离状态及可尝试的选项。
- [x] **简化真实歌曲的歌词取得与人工对齐入口**
  - 制作页内置 Vmoe 官方 ASS 搜索页，在站点要求的 reCAPTCHA 验证后可下载逐字特效字幕；
  - QQ 音乐单曲链接可自动读取公开行级 LRC 和翻译，再结合本地音频精修逐字时间；
  - Vmoe、QQ 音乐、网易云和本地歌词统一从制作页进入校准编辑器，减少重复上传和页面切换。

## 已完成（0.5.0）

- [x] **可选 Demucs 安装与真实设备诊断**
  - 独立安装脚本可选推荐的 CPU 版或 NVIDIA CUDA 版，并始终保留 CPU 回退；
  - 网页和 `doctor` 显示实际设备，人声分离持续回报真实输出。
- [x] **真实素材自动编辑验证**
  - 自动模拟载入、试听、删词、撤销、拖动边界和全格式导出，不改写输入文件。

## 已完成（0.4.0）

- [x] **可视化逐词时间轴编辑器**
  - 制作页一次提交素材即可生成可试听的校准工程，并在确认后带回最终 MV 流程；
  - 支持拖动逐字边界、直接增删文字且保留其他词时间、播放头跟随、缩放、撤销/重做、
    循环/连续试听、空格暂停继续和实时 KTV 扫色预览；
  - 歌词总览可隐藏、恢复、插入或删除行，并保留可恢复的项目 JSON。

- [x] **提升模型和素材异常时的可恢复性**
  - Whisper 优先使用本机缓存；模型服务不可用时，`auto` 保留已有时间轴继续制作；
  - 精修会保留带时间戳的空白间奏行，不再误报歌词行丢失；
  - 未上传独立音频时自动检测并复用 MV 内嵌音轨，静音 MV 才要求补充音频。

## 已完成（0.3.0）

- [x] **在网页中手动修正注音**
  - 自动生成的日语振假名或英文片假名可能有多音字、专有名词和唱法误差；
  - 提供逐行、逐词注音编辑，并在字幕预览中立即显示修改结果；
  - 继续支持直接修改项目 JSON 中的 `pronunciation` 字段作为高级用法。

- [x] **删除或排除不需要的歌词**
  - 在时间轴界面中删除片头署名、口白、重复错误行或其他不希望显示的歌词；
  - 删除后自动维护原文、翻译、注音、上下双行配对和导出时间轴的一致性；
  - 最好同时提供“暂时隐藏”和“彻底删除”，避免误操作后无法恢复。

- [x] **让逐字时间精修策略完全由用户选择**
  - `关闭`：完全保留输入文件中的时间；
  - `自动`：仅精修普通 LRC/SRT 等行级时间，保留可信的增强 LRC/YRC 逐字时间；
  - `强制`：即使输入已有逐行或逐字时间，也按原句窗口检查演唱音频，仅采纳可靠修正；
  - 网页和命令行使用同一组选项，并清楚提示耗时及时间来源。

## 后续方向

- [ ] 独立的音素级声学对齐后端（区别于 0.12.0 的 Whisper token 级逐行 CTranslate2 对齐）；
- [ ] 更灵活的双语、多行字幕模板；
- [ ] Docker 镜像和可复现模型配置；
- [ ] 批量项目清单。
